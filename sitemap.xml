---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for page in site.pages %}
  {% unless page.url contains 'SITE_MAP_MANUAL' or page.url contains 'privacy-policy' or page.url contains '.json' or page.url contains '.css' or page.url contains '.js' or page.url contains '.txt' or page.url contains '.xml' %}
  <url>
    <loc>{{ page.url | absolute_url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>{% if page.url == '/' %}daily{% elsif page.url contains 'blog' %}weekly{% else %}monthly{% endif %}</changefreq>
    <priority>{% if page.url == '/' %}1.0{% elsif page.url contains 'introduction' %}0.9{% elsif page.url contains 'blog' %}0.8{% else %}0.7{% endif %}</priority>
  </url>
  {% endunless %}
  {% endfor %}
  
  {% for post in site.posts %}
  <url>
    <loc>{{ post.url | absolute_url }}</loc>
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
  {% endfor %}
</urlset>
